# CLAUDE.md — ملف الإحاطة لـ Claude Code

**اقرأ الملف ده بالكامل قبل أي خطوة.** ده مشروع AdLoop، بُني عن طريق محادثة طويلة جداً مع Claude في claude.ai، وده ملخّص كل حاجة مهمة عشان الاستمرارية تكون كاملة من غير ما يتفقد أي سياق. (التاريخ التفصيلي يوماً بيوم مش هنا — راجع `git log` أو `SESSION-NOTES.md` لو محتاج قصة "إزاي وصلنا هنا"؛ الملف ده بيوثّق "إحنا فين دلوقتي" بس.)

## المشروع
AdLoop — "طبقة الحقيقة" لأصحاب الإعلانات: بيقارن الرقم اللي منصة الإعلانات
(جوجل/ميتا) بتقوله بالرقم الحقيقي المتحقق منه فعلياً (رسايل واتساب حقيقية،
مكالمات، تحويلات موثّقة) - مش عرض بيانات، كشف الفرق بين الظاهر والحقيقي.

مشروعين منفصلين:
- `adloop-saas/` — المنتج الرئيسي (Next.js **16.3.3**، Postgres على Neon، Prisma)
- `wa-conversion-tracker/` — تتبع واتساب. 🔴 **السطر ده كان بيقول
  «SQLite»، وده غلط من وجهين:** المتتبّعُ على **نفس قاعدة بيانات المنتج**
  (Postgres على Neon)، و**مافيهوش Prisma أصلاً** - لا مجلّد `prisma/` ولا
  شيما، بيكلّم القاعدة بـ`pg` وSQL خام.
  **والنتيجةُ العملية خطيرة:** `db push` بيمسح أيّ جدولٍ مش معرَّف في شيما
  المنتج، فأيُّ جدولٍ بينشئه المتتبّع بـSQL خام **لازم يتعرَّف في
  `adloop-saas/prisma/schema.prisma`** وإلّا كلُّ بناءٍ بيحذفه. - **متصل فعلياً
  بـ adloop-saas** عبر محرك الإسناد (`/api/attribution/*` endpoints):
  كليك بيتسجّل (`sync-click`)، تحقق حقيقي بيزوّد `verifiedConversions`
  فعلياً (`mark-matched`)، إسناد احتمالي للمحادثات المجهولة (`unattributed`).
  **والحلقة بتقفل فعلاً**: `lib/conversionSync.ts` بيرفع التحويل المتحقَّق
  رجوعاً للتلات منصات (Meta CAPI، TikTok Events API، Google
  uploadClickConversions) عبر كرون `/api/cron/conversion-sync` اليومي.

# Engineering Principles

You are the Staff Software Engineer responsible for this SaaS. Your primary responsibility is maintaining a production-grade system, not simply completing requested tasks.

## Before Every Change

1. Understand the existing architecture before making any modification.
2. Analyze how the requested change affects:
   - Authentication & Authorization
   - Workspace / Multi-tenancy
   - Database schema
   - APIs
   - Integrations (Google, Meta, TikTok)
   - Billing
   - AI services
3. If a better architectural solution exists, explain it before implementing.
4. Never introduce technical debt for the sake of speed unless explicitly requested.

## During Implementation

- Follow existing project architecture.
- Prefer reusable components over duplicated code.
- Keep code modular and maintainable.
- Write production-quality code, not prototypes.
- Preserve backward compatibility whenever possible.

## After Every Change

Perform a silent engineering review and verify:

- Security
- Performance
- Scalability
- Reliability
- Error Handling
- Logging
- Code Quality
- Technical Debt
- Type Safety
- API Consistency
- Database Integrity

## Critical Rule

If you discover a problem more important than the requested task (security issue, architectural flaw, scalability bottleneck, or data integrity risk), stop and explain it first before continuing.

## Completion Criteria

A task is NOT considered complete until it is production-ready.

Before finishing, always provide:

1. What changed.
2. Risks introduced (if any).
3. Follow-up improvements (optional).
4. Whether the implementation is Production Ready.

لا تنفذ أي ميزة قبل مراجعة تأثيرها على Architecture.
راجع Security قبل إنهاء أي مهمة.
افحص الأداء.
اقترح Refactoring إذا وجدت تكرار.
لا تكتب Quick Fix إذا كان الحل المعماري أفضل.
بعد كل Feature اعرض:
Risks
Technical Debt
Next Steps

## أوامر التشغيل (Commands)
- `npm run dev` — سيرفر التطوير (Next.js dev)
- `npm run build` — **إحدى وعشرون خطوةً بالترتيب، وأيُّ فشلٍ يوقف البناء كلَّه (قصدٌ لا باگ).** المرجعُ الحيّ هو `package.json`؛ والمجموعات:
  - **توليد:** `prisma generate` → `generateLogoManifest` → `generateSearchIndex` → `verify-nav-icons`
  - **اللغة (٤):** `checkTranslations` · `checkTranslationCoverage` · `checkArabicLeaks` · `checkBilingualFields`
  - **الأمان والصحّة (٧):** `checkWebhookAuth` · `checkCronAuth` · `checkPlatformCallsCounted` · `checkWorkspaceAccess` · `checkEmailSend` · `checkPlanLabels` · `checkReadinessCoverage`
  - **الفوترة والبحث:** `checkRenewalSafety` · `checkSettingsSearchIndex`
  - **القاعدة ثمّ البناء:** `db-push-safe` → `checkOrderPipeline` → `checkPlatformIngest` → `build-next`
  > **كلُّ فحصٍ في مجموعة الأمان وُلد من عطلٍ وقع فعلاً**، وسببُه مكتوبٌ في رأس ملفّه. أضِف الفحصَ في نفس اللحظة التي تضيف فيها ما يحتاجه - لا بعدها.
- `npm run start` — تشغيل نسخة الإنتاج المبنية
- `npm run db:push` — Prisma db push مباشر (بدون سلسلة الفحوصات، للتطوير السريع بس)
- `npm run db:studio` — Prisma Studio
- `npm run check:i18n` — فحصَي الترجمة بس (`checkTranslations` + `checkArabicLeaks`) بدون باقي سلسلة البناء
- **مفيش test script ولا lint script منفصل في `package.json`.** التحقق بيحصل عبر سلسلة فحوصات `build` + `npx tsc --noEmit` اليدوي (راجع "نظام صادق مع نفسه" تحت). لا تفترض وجود `npm test` أو `npm run lint`.
- بيئة النشر: Vercel (فريق `i-digital1`، مشروع `ad-loop`؛ والمتتبّع `wa-conversion-tracker`).
- 🔴 **`NODE_OPTIONS` ليس متغيّرَ بيئة ولا هو مضبوطٌ على Vercel** - وهذا السطر كان يقول غيرَ ذلك. `scripts/build-next.mjs` يحقن `--max-old-space-size=6144` في بيئة العملية قبل `next build`، والسببُ في رأس السكربت: صيغةُ `VAR=... command` لا تعمل على ويندوز فلزم سكربت. **لا تبحث عنه في لوحة Vercel ولا تضفه هناك** - غيابُه من قائمتها صحيح. (وSentry يلفّ كلَّ صفحةٍ ومسارٍ وقت البناء، ومن هنا جاء ضغطُ الذاكرة.)

## الحالة الحالية (آخر تحديث فعلي: 4 سبتمبر 2026)
**حدّث هذا القسم مباشرة عند أي تغيير جوهري — لا تضيف قسم "حالة" جديد تحته، عدّل هنا.**

> **كيف اتحدّث القسم ده:** الأرقامُ **معدودةٌ من الشجرة** (`find`/`grep`) لا
> منقولةٌ من نسخةٍ أقدم، وحالةُ متغيّرات البيئة **مقروءةٌ من `vercel env ls`**
> للمشروعين لا مفترضة. النسخةُ السابقة كانت من ١٨ أغسطس، وبينها وبين اليوم
> **٢٠٦ كوميت** - وأرقامُها كانت أقلَّ من النصف.

**المقاسات (معدودة ٤ سبتمبر):** ٩٠ صفحة · ١٥٦ مسار API · ٨٨ جدولاً في
`schema.prisma` · ٧ كرونات في `vercel.json` · ٤٠٨٥ مفتاح ترجمة × لغتين.
Next.js `16.3.3` · Prisma `5.x`.

**البنية التحتية:** جوجل وميتا وتيك توك التلاتة عندهم OAuth مبنيّ ومزامنة
يومية. نظام الدفع (Paymob) مبنيّ بالكامل — الفجوة الوحيدة ترتيبُ حقول HMAC
(تأكيدٌ يدويّ مطلوب بأوّل ربطٍ حقيقيّ).

> ⚠️ **مبنيٌّ ≠ قابلٌ للتشغيل.** `TIKTOK_APP_ID` و`TIKTOK_APP_SECRET`
> **غير مضبوطين في الإنتاج** (مؤكَّد من `vercel env ls`) - فكودُ تيك توك
> موجودٌ كلُّه ولا يقدر مشتركٌ واحد يربط حساباً. ونفس السؤال يتسأل على أيّ
> تكاملٍ تاني: راجع `lib/launchReadiness.ts` قبل ما تقول عن حاجة إنّها شغّالة.

**جوجل: 25 من 25 فئة مكتملة** — كل قائمة `user-questions-gap-analysis.md` خلصت.

**ميتا: تقدّم كبير، مش كامل.**
- ✅ كامل: فيسبوك/إنستجرام، الأماكن التفصيلية، استراتيجية المزايدة، الإعلان الفردي، المنافسين، نظرة شاملة، التقارير، التنبيهات، الإعدادات، شكل المحتوى، مسار العميل، التنبؤ والتخطيط، إيكومرس (بند 68 بس)
- ⚠️ جزئي: الثقة في البيانات، الميزانية، الجمهور، أول استخدام، جودة الحساب (2 من 3)، الإسناد (49 تم، 47/48 لسه)، فورم الليدز (جاهز تقنياً، محتاج صلاحيات)
- ❌ فاضي تماماً: **Partnership Ads (بند 76)، Page Quality (بند 78)، Meta Shops checkout tracking (بند 70)**
- 🚫 مؤجّل/مرفوض بقرار: الفريق والتعاون (رُفض صراحة)، أداء منتج بعينه جوه كتالوج (بند 69 — ميتا نفسها بتأكد إنه غير متاح كرؤية أصلية)

**تيك توك:** OAuth + مزامنة + خطّاف + تعب + Spark Ads + Cost Cap + فترة تعلّم + Lookalike + تعليقات + Scale/Kill + تدرّج مزايدة — كلها مكتملة **في الكود** (شوف التحذير فوق). Smart+ (items 6-10) مؤجّل لحد أول ربط حساب حقيقي.

**الربط بين المنتجين (adloop-saas ↔ wa-conversion-tracker):** محرك الإسناد شغال بالكامل (`/api/attribution/sync-click`, `mark-matched`, `unattributed`)، **والحلقة بتقفل**: `lib/conversionSync.ts` بيرفع التحويل المتحقَّق رجوعاً للتلات منصات — Meta CAPI (`v25.0`)، TikTok Events API (`v1.3`)، وGoogle `uploadClickConversions` — مع تهشيم SHA-256 لكل PII وتطبيع E.164 للهاتف وتقييم جودة المطابقة قبل الإرسال (`lib/matchQuality.ts`، وأي تحويل تحت `MIN_WORTH_SENDING_SCORE` بيتسكّب). الكرون اليومي `/api/cron/conversion-sync` هو اللي بيشغّلها (`syncPendingConversions`).
> والمتتبّعُ على قاعدة البيانات الجديدة (`DATABASE_URL` اتحدّث ٤ سبتمبر)، وبيشارك نفس القاعدة مع المنتج — فأيّ جدولٍ خام لازم يتعرَّف في الشيما وإلّا كلّ بناءٍ بيمسحه.

> ⚠️ التوثيق ده كان بيقول لفترة إنّ `sendMetaConversion`/`sendTikTokConversion` مش مبنيين - **وده كان غلط**. الاسمين مش موجودين لأنّ التنفيذ اسمه غير كده، والكرون بيستورد الملف بـ`await import()` ديناميكي فالـgrep الساكن مابيشوفوش. **grep على اسم دالة متوقَّع مش دليل غياب** - دوّر على السلوك (`graph.facebook.com`، `uploadClickConversions`) مش على الاسم اللي في دماغك.

**اللي اتبنى بين ١٨ أغسطس و٤ سبتمبر (٢٠٦ كوميت) — العناوين الكبيرة:**
- **صندوق دعمٍ موحَّد** (واتساب + ماسنجر + شات الموقع) بملاحظاتٍ داخلية ووسومٍ وتعيين — التفصيل تحت.
- **تقييمُ الخدمة من العميل** (`SupportRating`) وتحليلُه لكلّ مسؤول في اللوحة.
- **طابورُ مبيعاتٍ منفصل** (`SalesEnquiry`) بحالاتٍ خاصّةٍ به، غير طابور الدعم — **وطلبٌ مفتوحٌ واحدٌ لكلّ عميل**: طلبٌ ثانٍ بينما الأوّل `NEW`/`CONTACTED` بيترفض، وسقفُ ١٤ يوماً بيرفع المنع لأنّ طلباً مفتوحاً أسبوعين تقصيرٌ **منّا** لا منه.
- **الشروط وسياسة الخصوصية**: بنداً بنداً لحماية الشركة (B2B صراحةً، تحكيمٌ مُلزِم بجلساتٍ عن بُعد والخاسرُ يدفع، شرطُ جزاءٍ متّفَق، محاكم قنا، أدوارُ المتحكّم/المعالِج، الضريبة).
- **الباقاتُ حقيقية**: كلُّ وعدٍ في الجدول مربوطٌ بحدٍّ مطبَّق، وعرضُ إطلاقٍ بسعرٍ أساسيٍّ **يُتقاضى فعلاً** عند إطفاء المفتاح (`pricing.launchOffer`).
- **مركزُ مساعدة**: ٥١ مقالاً في ١٠ أقسام (كان ١٤ في ٤).
- **`lib/sendEmail.ts` نقطةُ الخروج الوحيدة لأيّ بريد** — التفصيل تحت.
- **نسخٌ احتياطيّ ليليّ** (٢٥ جدولاً، كان ٤) بمسارِ استرجاعٍ لا يمسح شيئاً.
- **مقاعدُ الفريق**: أربعةُ فلاتر وصول، و٢٤ مسارَ كتابةٍ كانت على فلتر القراءة اتصلّحت.
- **تحقّقٌ بخطوتين يُدار بالعامل نفسه** (`lib/mfaStepUp.ts`) — التفصيل تحت.
- **شيت جوجل حيّ** للتصدير بلا تكاملٍ يُبنى، **وتنبيهُ استهلاكٍ شاذّ** للذكاء بيوصل بالبريد.

**الأمان:** آخر تقييم كامل 83/100 (18 يوليو 2026) — **متوقّف ومتجاوَز بحجمٍ ضخم** من الكود بعده. **لازم مراجعة أمنية جديدة قبل أي إطلاق حقيقي**، مش الاعتماد على الرقم القديم. تفاصيل الحالة في `SECURITY.md`.

**الأوديت الشامل (23 أغسطس 2026) واتقفل (30 أغسطس):** `AUDIT-FINDINGS.md` — جولتان، 20 وكيل، 38 Critical. **الحكم اتحوّل من ⛔ NO-GO لـ✅ GO**. اقرا **القسم ٠** فيه قبل أي شغل على الأرقام أو الفوترة.
> **وبندٌ منه ظلّ مفتوحاً لحد ٤ سبتمبر: D-4** (راوتان حيّان لسلّة، والطلبُ بيتعدّ مرّتين لمن سجّل العنوانين). اتقفل: `/api/webhooks/salla` بقى `410 Gone`. **الدرس: «الحكم اتحوّل لـGO» مش معناه إنّ كلّ بندٍ اتنفّذ** - افحص البند نفسه قبل ما تعتمد عليه.

**تقرير Codex عن المتتبّع (٣١ أغسطس) واتحقّقنا منه (٢ سبتمبر):** الحكم بنداً ببند في `docs/codex-audit-verification-2026-09-02.md`. تلاتة صحّ واتصلحوا: الويب هوك كان بيبلع كل رسالة في الدفعة إلا الأولى، ونداءات الإسناد كانت `void` فبتموت لمّا الرد يمشي (بقت `after()`)، و`WaClick` كان بيخزّن الهاتف وIP للأبد (بقى بيتطمس بعد ٩٠ يوم). وبندان كان التقرير متأخّر فيهم عن الكود (PAY-1، وماكرو الحملة). **وتلاتة مستنيّة قرارك**: الروابط الموقَّعة، وحدّ المعدّل (الـIP مشترك مع CGNAT في مصر)، وصندوق الصادر.

**صندوق الدعم الموحَّد والمقاعد — مبنيّان وشغّالان:**
- **`lib/inbox.ts` نقطةُ الدخول الوحيدة لأيّ قناة.** واتساب وماسنجر وشات
  الموقع في صندوقٍ واحد (`/admin/support`)، وكلُّهم بيدخلوا من
  `ingestInboundMessage` - فقناةٌ رابعة بتاخد التعيين والوسم والبحث
  والإغلاق ببلاش. **الثابتاتُ في `lib/inboxChannels.ts` صفر استيراد**،
  لأنّ الملفّ الأصليّ بيجرّ `web-push`.
- **غيرُ المقروء مشتقٌّ لا معدود:** `readByAdminAt < lastMessageAt`.
  و`lastMessageAt` منفصلٌ عن `updatedAt` عمداً - التاني بيتحرّك بالوسم
  والتعيين، فالترتيب كان بيقفز بأفعالٍ مش رسائل.
- **الردّ يُكتب قبل ما يُوصَّل** (`lib/channels/outbound.ts`)، وفشلُ
  التوصيل بيرجع كتحذيرٍ على ردٍّ **محفوظ**. ونافذةُ الـ٢٤ ساعة بتاعت ميتا
  بتتفحص عندنا قبل النداء عشان «النافذة قفلت» تتميّز عن «الإعداد غلط».
- **الملاحظات الداخلية جدولٌ ومسارٌ منفصلان** (`SupportNote`) لا علامة
  على الرسالة: علامةٌ واحدة معناها إنّ أوّل شرطٍ ناقص بيبعت ملاحظةً
  داخلية للعميل، وده مالوش تراجع.
- **الربط من `/admin/channels` لا من متغيّرات بيئة** (`ChannelConnection`،
  السرّ مشفَّر). الصفحة بتفرّق بين «محفوظ» و«بيستقبل فعلاً»: توقيعٌ غلط
  بيترفض بصمت وشكلُه مطابقٌ لـ«محدّش كتب».
- **المقاعد: الوصول ليس الصلاحية.** أربعةُ فلاتر في `lib/workspaceAccess.ts`
  (وصول / كتابة / مالك / صفّ شخصيّ) - **والخطرُ في اختيار الغلط منها**،
  لأنّ مسارَ كتابةٍ على فلتر الوصول بيحوّل كلّ مقعد اطّلاع لمقعد تنفيذ.
  و`scripts/checkWorkspaceAccess.mjs` بيفشّل البناء على أيّ
  `POST/PATCH/PUT/DELETE` بيستعمل فلترَ الوصول. التفصيل في `SECURITY.md`.

**تغييرات معمارية مهمة (لا تُنقَض بلا سبب):**
- **`lib/metricRollup.ts` هو المصدر الوحيد لقراءة `MetricSnapshot`** — الجدول فيه مستويين (صف `ALL` مجمَّع + صفوف مقسَّمة على مكان الظهور)، وميتا بتكتب **واحد منهم مش الاتنين**. الجمع الخام بيعدّ صرف ميتا مرّتين، والفلترة لـ`ALL` وحدها بتخفي ميتا خالص. أي استعلام جديد يمرّ من هنا.
- **`ConversionVerification` هو مكان التحقق الحقيقي، مش `MetricSnapshot.verifiedConversions`** — الزيادة القديمة كانت `updateMany` على صف `ALL` اللي ساعات مايكونش موجود، فتضيع بصمت. القرّاء بيجمعوا الاتنين (جمع مش استبدال، عشان الديمو). و`adId` قيمته `"ALL"` مش `null` عمداً: بوستجرس بيعدّ الغائب متمايز في القيد الفريد، فـ`null` كان هيخلّي كل `upsert` يعمل صف جديد **فيتضاعف العدّ**.
- **تهشيم الهاتف مرّتين:** `hashPhone` (أرقام بس) لميتا، و`hashPhoneE164` (بعلامة `+`) لجوجل وتيك توك — موثَّق عند الاتنين، والتهشيم مابيغفرش محرف.
- **`finishCronRun` في `lib/cronRun.ts`** — كل كرون يرجّع رمز حالة صادق (200/207/500) ويكتب صفّه باسمه. الرد `200` الدائم كان بيخلّي يوم الفشل شكله زي يوم النجاح بالظبط.
- **`lib/sendEmail.ts` نقطةُ الخروج الوحيدة لأيّ بريد.** مكتبةُ Resend
  **لا ترمي استثناءً** عند الرفض - بترجّع `{ data, error }`. فكلُّ نداءٍ في
  المنتج كان جوّه `try/catch` **مابيتنفّذش أبداً**، ورفضُ المزوّد (نطاقٌ غير
  موثَّق مثلاً) كان بيعدّي **كنجاح**: ردٌّ ٢٠٠، وسجلٌّ صامت، ورسالةٌ مش
  موجودة. تلتاشر موضع كانوا شايلين نفس العطل، منهم استعادةُ كلمة السرّ
  وكودُ التحقّق بخطوتين. و`scripts/checkEmailSend.mjs` بيمنع الرجوع للنداء
  المباشر.
- **`lib/mfaStepUp.ts`: التأكيدُ بالعامل اللي صاحبُ الحساب مالكه فعلاً.**
  حسابُ جوجل/فيسبوك مالوش كلمةُ سرّ، فكان **ممنوعاً من إدارة تحقّقه
  بخطوتين** ومُحالاً لـ«تواصل معنا» - يعني إطفاءُ الحماية بقى محادثةَ دعمٍ
  يحكم فيها موظّفٌ بانطباعه. البديل: رمزٌ حاليّ من التطبيق المسجَّل، أو
  كودُ استرجاعٍ **بيتحرق**. وكلمةُ السرّ بتفضل مقبولة لمن له واحدة.
- **`lib/launchReadiness.ts` + `scripts/checkReadinessCoverage.mjs`:** كلُّ
  متغيّرٍ بيقراه الكود لازم له صفٌّ بيقول **إيه اللي بيقف لو غاب**. الملفُّ
  كان بيدّعي إنّ قائمته «مشتقّة من الكود» وهو مغطّي ٣٤ من ٥٠ - ومنها **سرُّ
  تسجيل الدخول بجوجل والمعرّفُ وحده مفحوص**، وفحصُ نصفِ زوجٍ مابيفحصش حاجة:
  شاشةٌ خضرا وتسجيلُ دخولٍ بيفشل بعد ما المستخدم يوافق.

**لسه ناقص فعلاً (الباقي غير مبني):**
1. **Partnership Ads، Page Quality، Meta Shops checkout tracking** (ميتا) — فاضيين تماماً
2. **TikTok Smart+** — مؤجّل لحد ربط حساب حقيقي
3. **مراجعة أمنية جديدة** قبل أي إطلاق حقيقي (الـ83/100 من يوليو)
4. **تأكيد ترتيب حقول HMAC** من لوحة Paymob قبل أيّ فلوس حقيقية
5. **إعدادٌ ناقصٌ على Vercel بيمنع ميزاتٍ مبنيّة**: توثيقُ نطاقٍ في Resend
   (البريد كلُّه واقفٌ عليه)، ومفاتيحُ تيك توك، وقيمُ العلامة والسوشيال.
   **المرجعُ الحيّ `lib/launchReadiness.ts` لا القائمة دي.**

> **بنودٌ كانت مكتوبةً هنا واتقفلت — متعيدش فتحها:**
> - ~~«الترجمة ~1200 سطر متبقي»~~ — **خلصت.** القاموس متكافئ (٤٠٨٥ مفتاحاً
>   × لغتين)، وفحوصُ اللغة الأربعة خضراء، والباقي دَينٌ موروثٌ واحد.
> - ~~«الديمو والتسعير قرارٌ غير نهائي»~~ — **اتبنى.** `Workspace.isDemo`
>   موجودٌ في الشيما، والباقاتُ والتسعيرُ وعرضُ الإطلاق شغّالين.

## اقرأ الملفات دي بالترتيب قبل أي تعديل
0. `adloop-saas/SECURITY.md` — **معمارية الأمان الكاملة، وثيقة حية.** أي ميزة جديدة تؤثر على الأمان لازم تُحدّث هنا قبل ما تُعتبر مكتملة
1. `adloop-saas/docs/ADR.md` — كل القرارات المعمارية والفلسفة (الأهم)
2. `adloop-saas/README.md` — الـ backlog الكامل بالتفاصيل
3. `adloop-saas/docs/activation-checklist.md` — كل مفتاح/حساب خارجي محتاجينه
4. `adloop-saas/docs/claude-api-usage-map.md` — تتبع تكلفة Claude API
5. `adloop-saas/docs/user-questions-gap-analysis.md` — قائمة 100 سؤال/فجوة خاصة بجوجل تحديداً (62-100 تغطي زوايا جديدة: Performance Max، يوتيوب، التسوق، جودة الإعلان بالتفصيل، الموسمية)، معظم الـ61 الأولى اتحلت
6. `adloop-saas/docs/meta-instagram-gap-analysis.md` — **الأساس الفعلي لخطة ميتا/إنستجرام** (85 سؤال مبنية على آليات ميتا الحقيقية، مش نسخة من قائمة جوجل) - أي شغل على ميتا لازم يرجع للملف ده
7. `adloop-saas/docs/tiktok-gap-analysis.md` — **الأساس الفعلي لخطة تيك توك** (63 سؤال) - **تحديث: OAuth تيك توك مبني ومستخدم فعلياً من زمان** (`syncTikTokAds.ts` وعشرات الميزات المبنية عليه - مزامنة، Scale/Kill، تدرّج المزايدة، كشف ترافيك مشبوه، فورم ليدز، إلخ). الفجوة المتبقية الوحيدة الموثّقة: TikTok Smart+ (items 6-10) - مؤجّلة لحد أول ربط حساب حقيقي نتأكد من حقول API.
8. `adloop-saas/docs/admin-panel-gap-analysis.md` — أساس لوحة تحكم المالك (`/admin`) - View As، تعليق حساب، سجل تدقيق، صحة النظام كلهم مبنيين فعلاً
9. `adloop-saas/docs/security-audit-2026-07-18.md` — **مراجعة أمنية شاملة** (BOLA، ثغرات مكتبات، قانون حماية البيانات المصري بموعد نهائي 31 أكتوبر 2026) - اقرأه قبل أي عمل على أي endpoint جديد

## قواعد أسلوب صارمة (اتفق عليها المستخدم بالتفصيل)
- **عربي مصري في المحادثة، فصحى/إنجليزي في أي نص هيظهر للمستخدم النهائي**
  (الواجهة، رسايل الخطأ، الإيميلات) - عربي مصري ممنوع في الكود نفسه
- **راجع كل حاجة قبل ما تقول "خلصت"** — شغّل `npx tsc --noEmit`، افحص إن
  الدوال الجديدة فعلاً متستخدمة (مش كود ميت)، افحص عدم التكرار
- **حدود صريحة، مش ادّعاءات** — لو حاجة مش شغالة كاملة أو فيها قيد من
  منصة خارجية، قولها بوضوح، متدّعيش إنها كاملة
- **بحث قبل أي تفصيلة تقنية خارجية** — إصدارات API، حدود منصات، أسعار -
  متفترضش من الذاكرة

## دروس وقرارات دائمة (Gotchas & Decisions)

**Next.js / بناء المشروع:**
- **المشروع على Next.js 16 دلوقتي (`16.3.3`).** تلات حاجات لازم تعرفها:
  - **الوسيط اسمه `proxy.ts` مش `middleware.ts`**، والدالة المصدَّرة `proxy` مش `middleware` — اصطلاح ١٦. الملف ده فيه حارسين مهمين (منع الكتابة أثناء «العرض كـ»، وحارس الأصل للكتابات) فمتنقلوش من غير ما تنقلهم.
  - **Turbopack هو المُجمِّع الافتراضي.** لازم `turbopack: {}` في `next.config.js` — وجود إعداد `webpack` بلا إعداد `turbopack` **بيفشّل البناء** أصلاً مش بيحذّر بس.
  - الحد الأدنى: Node ‏20.9 وTypeScript ‏5.1.
- Next.js 15 وما بعده يتطلّب `params: Promise<{...}>` في أي route أو `page.tsx` فيه `[id]` — الصيغة القديمة (Next.js 14) بتفشل بس وقت `next build` الحقيقي؛ `tsc --noEmit` العادي ما يمسكهاش. الخطأ بيظهر في `.next/types/validator.ts` (بيتولّد بس وقت build فعلي).
- `<html>`/`<body>` المفروض يكونوا في `app/layout.tsx` و`app/global-error.tsx` بس — أي صفحة تانية فيها نسخة خاصة بتعمل تعشيش HTML غلط.
- `app/layout.tsx` الجذري إجباري — من غيره التطبيق مبيتبنيش خالص.
- Client components ("use client") لازم ماتستوردش (حتى غير مباشر) من ملفات بتسحب مكتبات server-only (زي `google-ads-api` المحتاجة `fs`/`tls`/`net`) — استخرج أي ثابتات/types محتاجة في كلاينت كومبوننت لملف مستقل صفر استيراد.
- `tsc --noEmit` مش ضامن يمسك أسماء حقول Prisma غلط لو `prisma generate` فشلت (عزل شبكة) — تأكد بـgrep يدوي لو في شك.
- Sentry بيلف كل صفحة/route وقت `next build` — لو `SENTRY_ORG` مش مضبوط، بيتخطى اللف كله (يمنع SIGKILL/OOM وقت النشر). `NODE_OPTIONS=--max-old-space-size=6144` طبقة حماية إضافية.
- **قبل أي رد "خلصت":** شغّل `npx tsc --noEmit --project tsconfig.json` فعلاً + grep على اسم أي دالة جديدة تتأكد إنها مستخدمة (راجع "نظام صادق مع نفسه" تحت).

**أمان:**
- توكنات OAuth (جوجل/ميتا) — وصول مباشر لحسابات إعلانات العملاء الحقيقية — مشفّرة في قاعدة البيانات (AES-256-GCM عبر `lib/encryption.ts`)، مش نص صريح. محتاج `TOKEN_ENCRYPTION_KEY` في متغيرات البيئة (موثّق في `activation-checklist.md`). أي نقطة قراءة/كتابة جديدة للتوكنات لازم تمر من الملف ده، مش تخزين/قراءة خام.

**الأرقام في العربي (قاعدة ثابتة):**
- **كل رقم بيانات = أرقام لاتينية (0-9)، حتى في الواجهة العربية.** المنتج كله أرقام، وخلط ٢٤٬٩٩٥ مع 24,995 في نفس الشاشة بيبان إهمال. الصيغة العربية بتتاخد من `"ar-EG-u-nu-latn"` مش `"ar-EG"` — بتدي أسماء شهور وعملة عربية بأرقام لاتينية.
- النثر العادي في القاموس (زي «٣٠ يوماً» في جملة) ممكن يفضل هندي، بس **أي رقم جنبه رقم تاني على الشاشة لازم يكون لاتيني**.

**عملة/عتبات:**
- ممنوع رقم صرف ثابت بعملة مجهولة في أي تنبيه — استخدم `lib/relativeSpendThreshold.ts` (نسبة من إجمالي صرف الـWorkspace، نفس العملة تلقائياً).

**تكلفة Claude API:**
- كل نداء Claude (تحليلات، فحص جودة الصور، الفحص العميق للصفحة) بيمر على `lib/aiRateLimit.ts` **قبل** التنفيذ — سقف شهري مشترك لكل مستخدم (مش لكل Workspace، ومش لكل استدعاء)، والكرون اليومي بياخد من نفس الرصيد المشترك مع الزرار اليدوي — لو نسيت الفحص ده على نداء جديد، الكرون بيستهلك بلا سقف. الرقم الحيّ وتفصيل التوزيع في `docs/claude-api-usage-map.md`، مش هنا (بيتغيّر مع تحديثات الـSDK/التفكير).

**Scale/Kill/Watch (القيم النهائية بعد بحث في ممارسات ميديا باير محترفين — 8 مصادر):**
- حد أقصى آمن لزيادة الميزانية: **20%** (`SAFE_SCALE_INCREASE_PCT`)
- `MIN_CONVERSIONS_FOR_KILL = 5`، `MIN_CONVERSIONS_FOR_SCALE = 20` (عتبتين مختلفتين عمداً)
- فترة راحة 3-4 أيام بين كل زيادة ميزانية والتانية (منفصلة عن cooldown التنبيه العام 7 أيام)
- الإعلان لازم يثبت نفسه 4 أيام نشاط مختلفة على الأقل قبل Scale (3 أيام لـKill)
- إعلان متعب (`fatiguedAdIds` من `rankCreatives`) ما يترشحش لـScale حتى لو تكلفته كويسة
- تأكيد ترتيب نسبي (أرخص/أغلى 30%) مطلوب قبل أي قرار، مش بس بُعد عن المتوسط
- ROAS إشارة تأكيد/نقض إضافية مش بديل عن CPA — مؤكد رسمياً لجوجل (`conversions_value`)، ثقة متوسطة لميتا/تيك توك
- لو `Workspace.profitMarginPct` محدّد، نقطة التعادل = `1 / (margin/100)` بتبقى فحص ربحية مطلق أقوى من أي مقارنة نسبية؛ من غيره يرجع للمقارنة النسبية

**تدرّج استراتيجية المزايدة:**
- جوجل: Max Clicks→Max Conversions عند 30 تحويل + عمر حملة 21 يوم، Max Conversions→Target CPA عند 30 تحويل تانية والرقم المقترح +12% فوق المتوسط الفعلي (مش تحته). رقم جوجل الرسمي (15 تحويل كحد أدنى) أقل من الممارسة المؤكدة بحثياً — لا تستخدمه.
- ميتا/تيك توك: نفس المنطق لكن عند 50 تحويل + هامش أمان 15% (أرقام مختلفة عمداً عن جوجل، مش نسخ)
- cooldown 14 يوم للتنبيه في الحالتين

**التحقق الحقيقي (verifiedConversions):**
- الزيادة الحقيقية بتحصل في مكانين بس دلوقتي: `/api/attribution/mark-matched` (واتساب) و`assessAndVerifyMessengerConversationsForWorkspace` (ماسنجر) — أي مسار تحقق جديد لازم يزوّدها هنا نفسه، **وبعد فحص وجود مسبق** (كان فيه باگ عدّ مزدوج حقيقي من نسيان الفحص ده على `mark-matched`).
- Meta Click-to-Messenger: يحتاج اشتراك webhook في `messages` **و**`messaging_referrals` مع بعض — `referral.ad_id` هو مكافئ `ctwa_clid` بتاع واتساب.
- فصل فيسبوك/إنستجرام الحقيقي (مش المُعلَن) يحتاج `site_source_name` (Meta Dynamic URL Parameter) مضاف يدوياً لكل إعلان — `fbclid` نفسه ما بيحملش معلومة المكان.

**تنفيذ الأتمتة (Apply) — ⚠️ الوصف القديم هنا كان غلط في الاتجاهين (W-6):**
- **مصدر الحقيقة الوحيد: `lib/executingActions.ts`.** فيه قائمة مُعدَّدة بكل `actionType` بيعمل نداء حقيقي على المنصة، مأخوذة حرفياً من الـ`switch` في `lib/actionFeed.ts`. أي `case` جديد يتضاف للقائمة في نفس اللحظة. **متكتبش القائمة هنا تاني** — القوائم اللي بتتكرر بتفترق.
- **التسعة اللي بينفّذوا فعلاً:** تدرّج المزايدة (3 منصات) · إيقاف إعلان فردي (3 منصات) · `PAUSE_CAMPAIGN` · `CHANGE_CAMPAIGN_BUDGET` · `APPLY_PRODUCT_PRICE`.
- **وفيه تلات أسطح كتابة بره فيد القرارات** كان التوثيق ساكت عنها تماماً: `POST /api/creatives/decision` (Scale بيرفع ميزانية الأب، Pause بيوقف الإعلان)، و`stockGuard` (بيوقف حملة كاملة عند نفاد المخزون)، و`automationRules` (بيغيّر ميزانية الحملة). **دي الحاجة اللي خلّت مسارين بيحرّكوا الميزانية بلا رقيب** — لأن كل قرار أمان تاني كان مبني على "تدرّج المزايدة بس".
- الاستشاري فعلاً (بيسجّل `APPLIED` بلا نداء خارجي): اقتراحات شكل المحتوى، وبنود Scale اللي `scaleKillAlerts` بيدفعها.
- **حكم المحرّك مُلزِم عند التنفيذ** (إصلاح W-4): `applyAdDecision` بيرفض إلا لو `view.decision === decision && view.executable`. قبل كده كان بينفّذ اللي الطلب بيسمّيه، فـ`{decision:"SCALE"}` على إعلان المحرّك قايل عنه `PAUSE` كان بيرفع ميزانيته.
- أي كتابة (mutate) على جوجل لازم تحمل `login_customer_id`/`managerAccountId` زي القراءة (بنية وكالة/MCC) — نسيانها بيفشل مع أي حساب تحت وكالة.
- `adGroupId` لازم يتخزّن صريح على `CreativeSnapshot` — لا تستنتجه من تقطيع `adId`.
- تأكيد بدرجتين قبل أي تنفيذ أتمتة حقيقي (دوسة أولى = "تأكيد؟" لمدة 4 ثواني، دوسة تانية = تنفيذ) — بدون modal أو باسورد.
- Kill آمن على مستوى الإعلان الفردي في الثلاث منصات. **Scale بينفّذ فعلاً** عبر `/api/creatives/decision` (مش معلوماتي زي ما كان مكتوب هنا) — وعشان كده بوابات `classifyScaleKillWatch` بقت مُلزِمة عند نقطة الكتابة لا عند رسم الزرار بس.

**حدود منصات مؤكَّدة بالبحث (لا تُعاد مناقشتها بلا مصدر جديد):**
- Meta Ad Library `ad_type=ALL` بيرجّع الاتحاد الأوروبي/بريطانيا بس (قيد DSA). مصر/الخليج → إعلانات سياسية بس. السحب الآلي مستحيل — عشان كده AdLoop بنى لوحة رصد منافسين داخلية بدل رابط لفيسبوك.
- تكلفة البضاعة غير موجودة في ويب هوك الطلب لأي منصة من الخمس. سلة → `cost_price` على المنتج. شوبيفاي → `InventoryItem.cost` (نداءان + صلاحية View product costs). زد/ووكومرس/إيزي → لا حقل أصلاً.
- ألوان المنصات الحقيقية: ميتا `#0866FF`، تيك توك `#FE2C55` (مالهاش أصفر — ده لون سناب شات)، جوجل مالهاش لون رسمي واحد (4 ألوان). مش شعارات حقيقية (حقوق ملكية) — شارات لونية بديلة.
- كشف الترافيك المشبوه هنا = تكرار IP + توقيع User-Agent بس (بيفحص `UnmatchedClick` و`CtaClickEvent` مع بعض) — صفر بصمة جهاز، صفر ML. حد مقبول واعٍ، مش أداة متخصصة.
- عمر الدومين: RDAP حقيقي عبر `rdap.org` (مجاني، بدون مفتاح)، عتبة 180 يوم.
- إشعارات الموبايل (Web Push): آيفون محتاج إضافة الموقع للشاشة الرئيسية أول (قيد من آبل) — أندرويد بيشتغل مباشرة.
- Google Ads API: طلب segments الجهاز والموقع الجغرافي في نفس الاستعلام بيحذف صفوف — لازم استعلامين منفصلين، مش دمج.
- إعلان مرفوض (disapproved) بيتحسب لجوجل بس (`countDisapprovedGoogleAds`، عبر `ad_group_ad.policy_summary.approval_status`) — ميتا وتيك توك مفهوم الرفض عندهم مختلف، فجوة معروفة مش مبنية.

**قرارات مؤجّلة/مرفوضة بوعي (لا تُبنى بلا نقاش صريح جديد):**
- نظام صلاحيات/فريق (Team & collaboration) — مرفوض صراحة من المستخدم.
- TikTok Smart+ (items 6-10) — مؤجّل لحد ربط حساب تيك توك حقيقي (فحص الحقول الخام، مش تخمين من تسمية طرف تالت).
- أداء منتج بعينه جوه كتالوج ميتا (بند 69) — ميتا نفسها بتأكد إنه غير متاح كرؤية أصلية.
- Paymob: ترتيب حقول HMAC تخمين من نمط توثيق قديم — لازم تأكيد من لوحة تحكم Paymob قبل أي فلوس حقيقية. بوابة عميل ذاتية الخدمة (إلغاء اشتراك) غير مؤكدة عند Paymob.
- تعليقات Spark Ads (تيك توك): كشف سبام بقواعد regex ثابتة بس، **صفر AI بقرار صريح من المستخدم** — جزء من صفحة Spark Ads، مش ميزة مودريشن منفصلة.

**كود ميّت / دوال معزولة معروفة (متأكَّد منها، لا تُفترض عاملة):**
- `driver.js` في `package.json` — صفر استخدام فعلي (تأكيد بـgrep). أي "جولة تعريفية تفاعلية" موثّقة في تاريخ المشروع القديم **لم تُبنَ فعلياً أو غير موصولة** — تأكد بgrep قبل الاعتماد عليها.
- `getMultiTouchRate` — بتقرا من جدول فاضي عملياً لحد ما `resolveSessionConversion` توصل.
- `computeRealResponseTimeMinutes` — صفر تتبّع على مستوى الرسالة في المشروع كله.
- `runFullPricingSafetyNet` — الطبقة الأعمق منها محتاجة جدول Order/Sale عام غير موجود في الـschema خالص (بُني `Product.sku`/`ProductSaleEvent` كحل أخف، لكن مفيش جدول Order كامل).
- `videoMetrics.ts` — جوجل بس بيملّي البيانات فعلياً؛ ميتا/تيك توك مش بيسحبوا لنفس الحقول.
- `NormalizedOrderItem.unitCost` — مصمَّم، صفر استخدام، تُرك عمداً (مفيش منصة بتبعت التكلفة في الطلب).

**تصميم:**
- اتجاه "الشاهد" (The Witness) هو الاتجاه المُختار من 5 مقترحات — التحقق (verification) هو اللغة البصرية لكل رقم، مش تفصيلة مدفونة. `TrustNumber.tsx` + `MetricCard`'s `verified?` prop هما نقطة التطبيق.
- كروت KPI: تينت خلفية خفيف (6%) بلون دلالي + مربع أيقونة ملوّن — الألوان دلالية دايماً، مش زخرفة/قوالب SaaS جاهزة.
- القائمة الجانبية: `lib/navConfig.ts` مصدر الحقيقة الوحيد لأي قسم بأقسام فرعية (توسّع تلقائي حسب المسار)، عدا الإعدادات (مستثناة صراحة). حالة الطي والـ`<aside>` لازم يعيشوا في نفس الكومبوننت (الكلاينت) — فصلهم بيكسر تضييق الإطار الفعلي حتى لو الطي شغال بصرياً.

## نظام صادق مع نفسه
قبل أي رد بـ"تم/خلصنا"، شغّل فحص حقيقي (مش افتراض):
```bash
npx tsc --noEmit --project tsconfig.json
grep -rln "اسم_الدالة_الجديدة" --include="*.ts" --include="*.tsx" . | grep -v node_modules
```
لو دالة معرّفة ومفيش نتيجة من الـ grep التاني (غير ملفها هي)، يبقى مش
متربطة فعلياً - متقولش "خلصت" قبل ما تصلحها.
